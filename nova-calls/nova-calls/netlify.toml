[build]
  base = "nova-calls/nova-calls"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10.5.0"
  NPM_CONFIG_REGISTRY = "https://registry.npmjs.org/"

[[plugins]]
  package = "@netlify/plugin-nextjs"
